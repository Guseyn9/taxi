# E2E-тесты ТДМ

| Файл | Сценарий |
|---|---|
| `standard-order.spec.ts` | **TEST-E2E-002 — А.1.1, стандартный вызов** (см. ниже) |
| `voting-order.spec.ts` | **TEST-E2E-003 — А.1.2, голосование** (см. ниже) |
| `driver-boarding.spec.ts` | TEST-E2E-001 — посадка по коду в голосовом заказе |
| `offer-order.spec.ts` | **TEST-E2E-004 — А.1.3, предложение** (см. ниже) |
| `offer-driver-decline.spec.ts` | **TEST-E2E-005 — А.1.4, отказ выбранного водителя** (см. ниже) |

Все идут против **живого backend** (`https://ibronevik.ru/taxi/c/gruzvill/api/v1`).

---

# TEST-E2E-001 — посадка по коду в голосовом заказе

Playwright-прогон полного пользовательского сценария против **живого backend**
(`https://ibronevik.ru/taxi/c/gruzvill/api/v1`). Закрывает дефект
ТЗ-DRIVER-BOARDING-001: после подтверждения кода посадки бэкенд переводил заказ
в `Started`, а интерфейс водителя оставался в режиме «Код посадки».

## Запуск

```bash
cd taxi
npm run test:e2e                              # все тесты
npm run test:e2e -- --project=standard-order  # только А.1.1
npm run test:e2e -- --project=voting-order    # только А.1.2
npm run test:e2e -- --project=driver-boarding # только посадка по коду
npm run test:e2e:report                       # HTML-отчёт последнего прогона
```

Dev-сервер Playwright поднимает сам (`npm start`). Если он уже запущен —
переиспользует; чтобы указать другой адрес, задайте `E2E_APP_URL`.

Первый запуск требует браузера: `npx playwright install chromium`.

## Учётные данные

Тест работает с настоящими учётками — в репозиторий они не попадают. Создайте
`taxi/.env.e2e.local` (он в `.gitignore`) или задайте переменные окружения:

```
E2E_DRIVER_LOGIN=...
E2E_DRIVER_PASSWORD=...
E2E_DRIVER_TYPE=e-mail
E2E_DRIVER2_LOGIN=...
E2E_DRIVER2_PASSWORD=...
E2E_DRIVER2_TYPE=e-mail
E2E_PASSENGER_LOGIN=...
E2E_PASSENGER_PASSWORD=...
E2E_PASSENGER_TYPE=e-mail
E2E_API_BASE=https://ibronevik.ru/taxi/c/gruzvill/api/v1
```

Подойдут те же тестовые аккаунты gruzvill, которыми пользуется
`driver-emulator` (`driver-emulator/config.json`): водитель из `drivers[]` и
пассажир из `orderGenerator.clients[]`. У водителя должна быть привязанная
машина — из неё берётся код посадки.

`E2E_DRIVER2_*` — **второй водитель, отдельная учётка со своей машиной**. Он нужен
только голосованию (TEST-E2E-003): смысл А.1.2 в том, что один заказ одновременно
доступен нескольким водителям, и одной учёткой этот сценарий не воспроизводится.
Тест проверяет, что `E2E_DRIVER2_LOGIN` указывает не на того же пользователя, что
`E2E_DRIVER_LOGIN`, и падает, если учётка одна и та же.

Если `E2E_DRIVER2_*` не заданы, вход второго водителя в проекте `setup`
пропускается, и остальные тесты работают как раньше. Сам голосовой тест при этом
падает — молча пропускать сценарий он не должен.

## Что настоящее, а что нет

Настоящее: frontend, Taxi API, состояние заказа, переходы, обе роли.
**Ни один endpoint сценария не подменяется** — ни создание заказа, ни
`set_performer`, ни `set_arrive_state`, ни `set_start_state`.

Единственный мок — тайлы карты (внешний тайловый сервер к посадке отношения не
имеет, §4 ТЗ).

Что делает браузер: смотрит заказ, откликается, едет, отмечает прибытие, вводит
код и подтверждает. Что делает тест напрямую через API: готовит данные
(пассажир создаёт голосовой заказ и выбирает водителя) и **независимо** от UI
читает состояние заказа для проверок. Redux из теста не трогается, шлюз не
вызывается, backend-команды вместо клика не отправляются.

`?driverEmulator=1` в адресе — штатный переключатель приложения
(`tools/emulatorMode.ts`), без него список заказов водителя принудительно пуст.
Это режим приложения, а не мок API.

## Тесты

| Тест | Что проверяет |
|---|---|
| A | правильный код → backend `Started`, интерфейс `Started`, форма посадки закрылась |
| B | код неверной формы → команда не уходит, заказ остаётся `Arrived`, интерфейс в режиме посадки |
| C | двойное нажатие «Подтвердить» → одно корректное итоговое состояние |
| D | перезагрузка страницы → интерфейс всё ещё `Started`, подтверждение кода не вернулось |

Состояние интерфейса читается атрибутом `data-driver-state` кнопки основного
действия, а не переводом подписи: подписи приходят из конфигурации бэкенда и
зависят от языка, состояние — нет.

## Почему в тесте A есть верхняя граница по времени

Дефект был не в том, что интерфейс **никогда** не догонит бэкенд, а в том, что
он догонял его только следующим опросом списка заказов. Первая версия теста без
границы **проходила на сломанном коде** — это проверено инъекцией регрессии.

Поэтому тест A замеряет, за сколько интерфейс приходит в `Started`, и требует
уложиться в `UI_SYNC_BUDGET_MS` (1.5 с) — заведомо меньше периода опроса
активных заказов (5 с) и отложенного refresh (1.5 с). Уложиться можно только за
счёт локальной синхронизации.

Проверка инъекцией регрессии (полный откат исправления — синхронизация store в
карточке, оптимистичное состояние карты и отметка подтверждённой посадки):

```
Expected: 5   (Started)
Received: 4   (Arrived)
```

То есть тест ловит ровно ту картину, что описана в ТЗ: `Backend = Started`,
`UI = Arrived`.

Откат **только** карточной синхронизации или **только** механизмов карты тест не
ловит — оставшегося пути хватает, чтобы уложиться в бюджет. Точечно каждый
механизм сторожат unit-тесты (`driverBoarding.test.ts`,
`DriverBoardingProjection.test.js`, `driverOrderState.test.ts`).

## Что выяснилось про backend (важно для §16 ТЗ)

На конфигурации `gruzvill`:

1. **Код посадки бэкендом не проверяется.** `set_start_state` с заведомо
   неверным `b_driver_code` возвращает `success` и переводит водителя в
   `c_state = 5`. Проверено напрямую по API.
2. **`b_driver_code` в заказе не возвращается.** Приложение сверяет введённый
   код с `order.b_driver_code` (`tools/driverBoarding.ts`), и когда его нет —
   принимает любой код правильной формы (3–4 цифры).
3. Передача `b_driver_code` в `set_performer` отклоняется («wrong driver code»),
   что совпадает с комментарием в `src/API/order.ts` и безопасным ретраем там же.

Поэтому требование ТЗ «неверный код → `Command = FAILED`, `Backend ≠ Started`»
на этом бэкенде невыполнимо: отвергнуть можно только код неверной **формы**, и
делает это фронтенд. Тест B проверяет ровно эту, настоящую гарантию — и не
имитирует недостающую серверную проверку моками. Валидацию кода на сервере
стоит завести отдельной задачей.

---

# TEST-E2E-002 — А.1.1, стандартный вызов

Первый сквозной тест на стандартный вызов такси. Он же — каркас для А.1.2
(голосование, реализовано в `voting-order.spec.ts`) и А.1.3 (предложение,
реализовано в `offer-order.spec.ts`).

## Запуск

```bash
cd taxi
npm run test:e2e -- --project=standard-order
```

Учётные данные — те же, что у TEST-E2E-001 (раздел выше). Дополнительно нужен
пассажир: проект `setup` теперь входит **обеими** учётками и складывает
`e2e/.auth/driver.json` и `e2e/.auth/passenger.json`.

## Две независимые сессии

```
browser
├── passengerContext (storageState: e2e/.auth/passenger.json) → passengerPage
└── driverContext    (storageState: e2e/.auth/driver.json)    → driverPage
```

Два разных пользователя — два разных контекста. Одной сессией два пользователя
не изображаются.

## Сценарий и проверки

| Шаг | Действие | Проверка |
|---|---|---|
| подготовка | водитель на линии, список активных заказов пассажира очищен, заказ создан через API | — |
| 1 | — | заказ создан: бэкенд отдаёт его по номеру |
| 2 | — | тип **А.1.1**: нет `b_voting`, нет услуги `5`, `b_cars_count ≠ 0` |
| 3 | пассажир открывает `/passenger-order` | заказ виден пассажиру в списке активных |
| 4 | водитель открывает список заказов | доступен **именно этот** заказ (карточка по `b_id`) |
| 5 | водитель жмёт «Взять заказ» | — |
| 6 | — | тип **А.1.1** (вторая опора): водитель сразу `Performer`, а не `Considering` |
| 7 | — | исполнитель ровно один, и это наш водитель |
| 8 | пассажир выбирает заказ | пассажир видит назначенного водителя (`Performer`) |
| 9 | водитель: вкладка «Карта» → «Поехал» | backend и интерфейс водителя в `Arrived`; пассажир видит `Arrived` |
| 10 | водитель: «Приехал» | backend и интерфейс водителя в `Started`; формы кода посадки не было |
| 11 | — | пассажир видит начало поездки (`Started`) |

### Почему тип заказа проверяется дважды

Признаки типа в самом заказе (`b_voting`, услуга `5`, `b_cars_count`) — это то,
как приложение различает три режима (`src/tools/driverOffer.ts`). Проверяются они
по контракту, а не по строке: `b_cars_count = 0` — это «предложение», у
стандартного вызова поле либо отсутствует, либо содержит положительное число
машин; нечисловое значение проверку тоже не проходит.

Оговорка про `b_cars_count`: **на gruzvill этот признак не работает.** Заказ,
созданный с `b_cars_count = 0`, читается обратно как `"1"` — проверено и числом,
и строкой. То есть отличить А.1.3 полями заказа на этой конфигурации нельзя, и
ветка `isOfferOrder` по `b_cars_count` (`src/tools/driverOffer.ts`) здесь не
срабатывает. Сверка контракта в тесте оставлена на случай, если бэкенд начнёт
хранить признак, но опираться в различении типа приходится не на неё.

Сильнее признаков — проверка поведением: после клика «Взять заказ» тест ждёт на
бэкенде именно `Performer`. Голосование и предложение завели бы водителя
кандидатом (`Considering`), и исполнителем его сделал бы только выбор пассажира
(`src/API/order.ts`) — которого в этом сценарии нет. То есть на голосовом заказе
ожидание не дождалось бы `Performer` никогда.

Обе проверки не зависят ни от подписи кнопки, ни от языка интерфейса.

### Что именно проверяется под словом «назначен»

`order.drivers` — это список **записей участия**, а не список исполнителей: у
записи есть и `c_becomed_candidate`, и `c_canceled`, то есть отказавшийся
водитель остаётся в заказе записью с `c_state = Canceled`
(`EBookingDriverState`, `src/types/types.ts`). Проверено на живом бэкенде.

Поэтому тест не требует, чтобы список состоял из одной записи — это была бы
проверка внутреннего устройства бэкенда. Проверяется бизнес-инвариант:

* исполнитель ровно один, и это наш водитель (`c_state === Performer`);
* рядом нет другого водителя, который всё ещё в игре, — то есть в любом
  состоянии, кроме `Canceled`.

## Что настоящее, а что нет

Настоящее: frontend, Taxi API, состояние заказа, переходы FSM, обе роли.
Ни один endpoint сценария не подменяется — ни `set_performer`, ни
`set_arrive_state`, ни `set_start_state`. Единственный мок — тайлы карты.

Через API делается ровно две вещи: **подготовка предусловия** (водитель на
линии, заказ создан) и **независимая от интерфейса проверка** состояния заказа.
Все действия сценария — клики в браузере.

Заказ создаётся тем же набором полей, что собирает форма пассажира для режима
`order` (`src/pages/Passenger/VotingForm.tsx`): без `b_voting`/`b_services`
голосования и без `b_cars_count=0` предложения. `set_confirm_state` не
отправляется — приложение шлёт его только для голосовых заказов.

### Объём теста: создание заказа — предусловие, а не покрытие формы

Решение принято осознанно и зафиксировано здесь, чтобы его не приняли за
упущение: шаг **«пассажир заполнил форму и нажал „Заказать“» этим тестом не
проверяется**. Заказ создаётся через API как fixture, и сценарий начинается с
уже созданного заказа.

Почему так:

* создание заказа из формы — это отдельная содержательная история: выбор точек
  на карте, класс авто, число мест, цена, комментарии, три кнопки режима
  (`Vote` / `Offer` / `Order`). Втащив её в первый сквозной тест, мы получили бы
  тест, который падает то на форме, то на поездке, и по которому непонятно, что
  именно сломалось;
* точки в форме ставятся кнопкой «точка на карте», а адрес к ним подтягивает
  внешний геокодер (Nominatim/HERE, `src/API/index.ts`) — ещё один внешний
  источник нестабильности поверх и без того нестабильного бэкенда;
* тип заказа при этом не остаётся на веру: он проверяется и по полям заказа, и
  по поведению FSM (см. выше).

**Следующий шаг:** отдельный E2E на создание заказа из пассажирской формы —
именно он закроет «пассажир создал заказ через UI». К нему же естественно
отнести проверку выбора режима (`Order` против `Vote` и `Offer`).

## Состояния в интерфейсе

Оба интерфейса отдают состояние числом, а не подписью:

* водитель — `data-driver-state` на кнопке основного действия (`pages/Driver/Map.tsx`);
* пассажир — `data-driver-state` на панели водителя (`pages/Passenger/VotingForm.tsx`).

Подписи приходят из конфигурации бэкенда и зависят от языка, состояние — нет.

## Изоляция и уборка

Заказ помечается `[DRV:<id>]` — механизм самого приложения, по которому водитель
в режиме внешнего эмулятора видит только свои тестовые заказы.

### Что уборка отменяет, а что не трогает

Уборка отменяет **только заказы с тестовыми метками** (`e2e/fixtures/taxiApi.ts`,
`isTestOrder`):

| Метка | Кто ставит |
|---|---|
| `[E2E <label>]` | эти тесты |
| `[DRV:<id>]` | driver-emulator, читается приложением (`src/tools/emulatorMode.ts`) |
| `[CASE] <имя>` | driver-emulator, имя тест-кейса |

Заказ без такой метки уборка **не трогает никогда** — она его пропускает и
называет в логе прогона (`E2E sweep: не тронуто заказов — …`). Это защита от
самой опасной ошибки конфигурации: если в окружении окажется чужой
`E2E_PASSENGER_LOGIN`, тест не отменит настоящие заказы настоящего пассажира.
Полагаться на «учётка ведь тестовая» здесь нечем — это молчаливое
предположение, а не гарантия.

Метка — единственный критерий, и у уборки после прогона он тот же, что у
уборки перед ним. Участие тестового водителя в заказе критерием отмены
сознательно не является: непомеченный заказ — это заказ настоящего пассажира,
и состав его участников прав на отмену уборке не даёт.

Обратная сторона: если на учётке остался мешающий заказ **без** тестовой метки,
уборка его не уберёт и сценарий может из-за него упасть. Такое падение
разбирается по строке `E2E sweep: не тронуто заказов` в логе — и это лучше, чем
незаметно отменить чужой заказ.

### Пагинация

Список активных заказов бэкенд отдаёт порциями (на gruzvill — по 5), причём
параметра смещения у endpoint нет. Обе уборки — и перед прогоном, и после
него (`cancelTestOrders`) — читают список повторно, пока порция приносит хоть
один отменяемый заказ.

Отсюда предел: порция, целиком состоящая из заказов без тестовых меток,
окно списка не сдвигает, и до тестовых заказов за ней уборка не доберётся. В
этом случае пропущенные номера тоже уходят в лог.

## Известная нестабильность: загрузка конфигурации приложения

Приложение при старте тянет конфигурацию отдельным скриптом с сервера
(`src/config.ts`): сначала `GET /api/v1/?cv=` за версией кэша, затем
`https://ibronevik.ru/taxi/cache/data_<config>.js`. Пока конфигурация не
загрузилась, весь интерфейс подменяется экраном «Database is unavailable»
(`src/Routes.tsx`).

**Повторной попытки в приложении нет.** Один сетевой сбой — и страница остаётся
на этом экране навсегда:

* если падает загрузка скрипта — `script.onerror` переводит конфигурацию в
  `Fail`, и это состояние видно в `data-config-status`;
* если не отвечает `?cv=` — промис отклоняется, скрипт вообще не добавляется, и
  приложение навсегда остаётся в `Loading`, без всякого признака ошибки.

Измерено на живом бэкенде: **1 сбой на 25 загрузок страницы** в спокойный период
и серии подряд в периоды деградации (в тех же прогонах бэкенд отдавал `HTTP 502`
и обрывал соединения). Каждая перезагрузка страницы — новая попытка, поэтому
тест сведён к минимуму перезагрузок: вкладка «Карта» открывается кликом по
вкладке, а не переходом по адресу.

Загрузка оболочки — и только она — повторяется из теста перезагрузкой страницы,
до трёх попыток (`e2e/fixtures/appShell.ts`). Это не обход проверок сценария:
ни один его шаг не пропускается, ни одно утверждение о состоянии заказа не
смягчается, а сценарий к этому моменту ещё не начинался — приложение просто не
открылось. Если оболочка не поднялась и с третьего раза, тест падает с явной
причиной, а не с немым таймаутом ожидания элементов, которых на этом экране нет.

Дефект при этом не заминается: каждая такая перезагрузка печатается в лог
прогона строкой `E2E APP BOOT FAILED` и попадает в аннотации теста, так что
видна и в зелёном прогоне. Починка — отдельная задача к приложению (таймаут и
повтор загрузки конфигурации); в этой ветке production-логика не менялась.

## Что настоящее, а что нет

Требование AC-14 ТЗ: явно разделить, что делает пользователь, а что — оснастка.

**Через интерфейс (браузером, кликами и вводом):**

* водитель открывает список заказов и находит карточку по номеру заказа;
* водитель открывает форму предложения, вводит цену, отправляет предложение;
* пассажир открывает карточку заказа и видит предложение;
* пассажир выбирает водителя («Выбрать»);
* водитель подтверждает окно «предложение принято»;
* **водитель отказывается от заказа** — действие и подтверждение в окне;
* пассажир перезагружает страницу;
* второй водитель открывает список, вводит цену и отправляет своё предложение.

**Через API — только это:**

* подготовка предусловий: авторизация, вывод водителей на линию, уборка
  предыдущих тестовых заказов, создание заказа;
* независимое чтение состояния заказа (`readOrder`, `performersOf`,
  `driverStateOf`, `offerPriceOf`, `orderDriverStates`, `isOrderActiveFor`);
* диагностика при падении и уборка после прогона.

**Ни одного API-вызова вместо действия пользователя.** В тесте нет ни
`set_performer`, ни `set_cancel_state`, ни `choosePerformer` — проверяется
`grep`-ом по файлу теста. Единственный мок на весь прогон — тайлы карты.

**Что подтверждается независимо от интерфейса:** каждый существенный переход
читается с бэкенда отдельным запросом — предложение и его цена, назначение
исполнителя, снятие участия после отказа, `b_state` заказа, отсутствие
исполнителя, активность заказа. Ни одна проверка сценария не опирается только на
текст в интерфейсе: состояния читаются атрибутами (`data-driver-state`,
`data-driver-id`, `data-offer-price`), а не переводимыми подписями.

## Ограничения backend, обнаруженные при разведке

Найдено замерами; на эти вещи тест опирается или сознательно не опирается.

| Что | Замер | Следствие для теста |
|---|---|---|
| `set_cancel_state` **не симметричен** | от водителя: `b_state → 1`, водитель → `Canceled`; от пассажира: `b_state → 3`, водитель остаётся `Performer` | инвариант строится на паре `b_state` + `c_state`, одного поля мало |
| запись участия при отмене заказа **не сбрасывается** | у отменённого пассажиром заказа водитель остался `Performer(3)` при `b_state = Canceled(3)` | «водитель не Performer» нельзя использовать как единственную проверку |
| предложение отказавшегося **сохраняется** | `c_options.performers_price` остаётся после перехода в `Canceled` | тест проверяет сохранение, а не исчезновение |
| водитель, отправивший предложение, **сам перестаёт видеть заказ** | `/drive/now` возвращает его второму водителю, но не первому — уже до выбора пассажира | сценарий не возвращается к списку заказов; для первого водителя это и не сработало бы |
| окно «предложение принято» поднимается **заново** при каждом монтировании | пока водитель `Performer` в offer-заказе | сценарий работает в уже открытой карточке, без навигации |
| узел окна результата **остаётся в DOM невидимым** после закрытия | со статусом, отличным от показанного | фикстура ищет окно по видимости, а не по наличию узла |
| из значений предложения backend возвращает **только цену** | `c_pickup_time`, `c_arrival_time`, `c_comment`, `driver_offer_price` — `undefined` (замер TEST-E2E-004) | независимо сверяются только цена и идентификатор водителя |

## Найденный дефект приложения: опрос списка заказов водителя встаёт навсегда

Обнаружен этим тестом, **в E2E-PR не исправляется** — это production-логика, и по
ТЗ такое выносится отдельной задачей. Здесь он описан потому, что напрямую влияет
на устойчивость всех водительских E2E.

```ts
// state/orders/sagas.ts
function* watchReadyOrdersSaga() {
  yield put({ type: GET_READY_ORDERS_REQUEST })
  yield take([GET_READY_ORDERS_SUCCESS, GET_READY_ORDERS_FAIL])   // ← ждёт одно из двух
  yield delay(...)
}

function* getReadyOrdersSaga() {
  if ((yield* select(userPrimaryCarSelector)) === null)
    return                                                       // ← не отправляет ни того, ни другого
  ...
}
```

Если на момент первого запроса списка машина водителя ещё не загружена,
`getReadyOrdersSaga` выходит молча. `watchReadyOrdersSaga` остаётся висеть на
`take` и **больше не просыпается никогда**: цикл опроса мёртв до перезагрузки
страницы, а список заказов у водителя остаётся пустым.

Это гонка загрузки, поэтому проявляется не каждый раз. Замерено: два прогона из
шести падали на том, что карточка заказа не появлялась у второго водителя за
120 с, притом что `/drive/now` этот заказ возвращал, а после перезагрузки
страницы карточка появлялась сразу.

**Как это обходит оснастка.** `openOrderCard` перезагружает страницу до трёх раз,
если карточка не появилась за 45 с, и пишет в лог `E2E DRIVER LIST STALLED`.
Селектор при этом один и тот же — это повтор загрузки, а не подбор селектора, и
предел жёсткий: после третьей попытки падение остаётся падением. Ровно тот же
приём, что уже применён к дефекту загрузки конфигурации (`expectAppBooted`).

## Атрибуты, добавленные в приложение

Только `data-*`, ни строки логики — тот же приём, что уже применён для кнопки
основного действия водителя:

| Файл | Атрибуты |
|---|---|
| `src/components/Card/OrderCard.tsx` | `data-testid="driver-order-card"`, `data-order-id` |
| `src/components/MiniOrders/index.tsx` | `data-testid="passenger-mini-order"`, `data-order-id` |
| `src/pages/Passenger/VotingForm.tsx` | `data-testid="passenger-driver-panel"`, `data-driver-state`, `data-driver-id` |
| `src/pages/Passenger/VotingForm.tsx` | `data-testid="passenger-voting-candidate"`, `data-driver-id`; `data-testid="passenger-voting-candidate-select"` |
| `src/components/modals/MessageModal.tsx` | `data-testid="message-modal"`, `data-message-status`; `data-testid="message-modal-close"` |
| `src/pages/Driver/index.tsx` | `data-testid="driver-tab-map"` |
| `src/Routes.tsx` | `data-testid="app-config-unavailable"`, `data-config-status` |
| `src/components/modals/CardModal.tsx` | `data-testid="driver-offer-open"`, `data-testid="driver-offer-price"`, `data-testid="driver-offer-send"` (А.1.3) |
| `src/pages/Passenger/VotingForm.tsx` | `data-testid="passenger-candidate-offer-price"`, `data-offer-price` (А.1.3) |

До этого во всём `src/` было пять `data-testid`, и все — водительские: у
пассажирского интерфейса и у карточки заказа в списке зацепиться за конкретный
заказ было нечем.

Что добавлено голосованием (TEST-E2E-003) и зачем именно атрибуты:

* **строка кандидата и кнопка «Выбрать».** Голосование заканчивается выбором
  пассажира, и выбрать надо конкретного водителя. В разметке кандидата не было
  ни его `u_id`, ни устойчивого селектора, а подпись кнопки приходит из перевода.
* **`data-driver-id` у панели водителя.** ТЗ требует сверить показанного
  пассажиру водителя с назначенным на бэкенде. Имя для этого не годится — оно не
  уникально и приходит из профиля.
* **окно результата действия.** Отклик на голосование подтверждается модальным
  окном, и до карты, пока оно открыто, не добраться. Исход читается атрибутом
  `data-message-status` (`success` / `warning` / `fail`), а не заголовком.

---

# TEST-E2E-003 — А.1.2, голосование

Сквозной сценарий голосования: один заказ, два водителя, выбор исполнителя
пассажиром.

## Запуск

```bash
cd taxi
npm run test:e2e -- --project=voting-order
```

Нужны все три учётки, включая `E2E_DRIVER2_*` (раздел «Учётные данные»).

## Три независимые сессии

```
browser
├── passengerContext (storageState: e2e/.auth/passenger.json) → passengerPage
├── driver1Context   (storageState: e2e/.auth/driver.json)    → driver1Page
└── driver2Context   (storageState: e2e/.auth/driver2.json)   → driver2Page
```

Общих cookies и localStorage у ролей нет.

## Сценарий и проверки

| Шаг | Действие | Проверка |
|---|---|---|
| подготовка | оба водителя на линии, тестовые заказы пассажира убраны, заказ создан через API | — |
| 1 | — | заказ голосовой: `b_voting = 1`, услуга `5`, `b_state = 1`, исполнителя нет |
| 2 | пассажир открывает `/passenger-order` | заказ виден пассажиру |
| 3 | водитель 1 открывает список заказов | карточка **именно этого** заказа найдена по `b_id` |
| 4 | водитель 2 открывает список заказов | тот же заказ доступен и ему (**AC-2**) |
| 5 | — | до откликов исполнителя нет, заказ в состоянии поиска |
| 6 | оба водителя жмут «Готов поехать» | приложение подтвердило отклик; на бэкенде оба `Considering` |
| 7 | — | **AC-3**: кандидаты появились, но исполнителя нет и `b_state` не изменился |
| 8 | пассажир жмёт «Выбрать» у водителя 1 | водитель 1 → `Performer`, `b_state = 2` |
| 9 | — | исполнитель ровно один; проигравший не `Performer` |
| 10 | — | пассажиру показан водитель с тем же `u_id`, что на бэкенде |
| 11 | водитель 1: карта → «Поехал» | backend, интерфейс водителя и пассажира — `Arrived` |
| 12 | водитель 1: код посадки | backend, интерфейс водителя и пассажира — `Started` |
| 13 | — | поездку выполняет ровно один водитель — выбранный |

## Чем голосование отличается от А.1.1 в самом тесте

**Заказ создаётся без метки `[DRV:<id>]`.** Метка — механизм изоляции самого
приложения: в режиме внешнего эмулятора водитель видит только свои помеченные
заказы (`state/orders/selectors.ts`, `filterReadyOrdersByTrainingDriver`).
Голосованию нужно ровно обратное. Уборка от этого не страдает: она работает по
метке `[E2E <label>]`, которая остаётся (см. «Что уборка отменяет»).

Проверено на бэкенде: `[DRV:]` — фильтр **клиентский**. В `/drive/now` водителю
приходят и помеченные чужим id заказы; прячет их приложение.

**Заказ создаётся с `b_max_waiting = 900`.** Пассажирский интерфейс доверяет
этому полю только в окне `0 < x ≤ 900` и иначе считает ожидание равным 180 с
(`VotingForm.tsx`, `getChoiceOrderMaxWaitingSeconds`), после чего **сам отменяет
голосовой заказ** по истечении времени. Сценарий из тринадцати проверок в 180 с
не укладывается. 900 — обычное значение поля заказа: не мок, не подмена FSM.
Бэкенд его принимает и возвращает обратно без изменений.

**Выбор исполнителя — клик пассажира, а не вызов API.** Таймерного автовыбора у
приложения нет: исполнителя назначает `chooseCandidate` (`set_performer` с
`performer='1'` и `u_id`), и вызывается он только из кнопки «Выбрать» в списке
откликов. Поэтому шаг 8 сделан кликом, а не `choosePerformer` из фикстуры —
в отличие от TEST-E2E-001, где выбор пассажира был предусловием, а не предметом
проверки.

**Переход `Arrived → Started` идёт через код посадки.** Для голосового заказа
это часть контракта, а не обход: тот же путь проверяет TEST-E2E-001. Код
настоящий, водительский (`boardingCodeOf`).

## Что выяснилось про backend голосования

Замерено зондом на живом gruzvill до написания теста:

| Этап | `b_state` | `drivers[]` |
|---|---|---|
| заказ создан и подтверждён | `1` (Processing) | пусто |
| водитель откликнулся | `1` | `c_state = 1` (Considering) |
| пассажир выбрал водителя | `2` (Approved) | `c_state = 3` (Performer) |
| водитель отметил прибытие | `2` | `c_state = 4` (Arrived) |
| поездка началась | `2` | `c_state = 5` (Started) |

Отсюда две опоры теста: состояние заказа (`b_state`) и состояние участия
(`c_state`) — обе числовые, от языка интерфейса не зависят.

`remaining_lifetime_seconds` в ответе `/drive/get/{id}` **не приходит**, поэтому
оставшееся время голосования тест не проверяет — приложение считает его само от
`b_created` и `b_max_waiting`.

## Состояние проигравшего кандидата

`drivers` — это записи участия, а не список исполнителей: отказавшийся водитель
остаётся в заказе записью. Поэтому тест проверяет не отсутствие записи, а её
состояние: проигравший не `Performer` и находится в одном из двух состояний,
которыми приложение описывает водителя вне поездки, — `Considering` или
`Canceled` (`types/types.ts`, `EBookingDriverState`).

Замерено на gruzvill: после выбора пассажира проигравший остаётся
**`Considering`** — бэкенд его не снимает и не отменяет. Заказ при этом уже
`b_state = 2`, а исполнитель ровно один, поэтому кандидатом он остаётся
формально: в поездку он не попадает, что тест и проверяет отдельно на последнем
шаге.

## Почему карта открывается переходом, а не вкладкой

У А.1.1 водитель нажимает «Взять заказ» и сразу становится исполнителем на той же
странице, поэтому там достаточно переключить вкладку (`switchToDriverMap`).

У голосования путь другой: после отклика приложение само уводит водителя с
карточки заказа (`CardModal.tsx`), а исполнителем он становится позже и **чужим
действием** — выбором пассажира. В этот момент клик по вкладке «Карта» до
основного действия не доводит: страница остаётся на списке, и
`driver-map-primary-action` не появляется. Поэтому карта открывается переходом
(`openDriverMap`) — тем же способом, которым её открывает TEST-E2E-001 для
голосового заказа.

---

# TEST-E2E-004 — А.1.3, предложение

Сквозной сценарий предложения: один заказ, два водителя со СВОИМИ условиями,
принятие предложения пассажиром и поездка до её начала.

Ниже — сценарий, затем инфраструктура под него, затем контракт, снятый с живого
gruzvill ДО написания теста, как требует ТЗ. Всё в разделах после
«Инфраструктуры» — **замеры**, а не чтение кода; где вывод сделан по исходникам,
это сказано отдельно.

## Запуск

```bash
cd taxi
npm run test:e2e -- --project=offer-order
```

Нужны все три учётки, включая `E2E_DRIVER2_*` (раздел «Учётные данные»).

## Три независимые сессии

```
browser
├── passengerContext (storageState: e2e/.auth/passenger.json) → passengerPage
├── driver1Context   (storageState: e2e/.auth/driver.json)    → driver1Page
└── driver2Context   (storageState: e2e/.auth/driver2.json)   → driver2Page
```

Общих cookies и localStorage у ролей нет.

## Сценарий и проверки

| Шаг | Действие | Проверка |
|---|---|---|
| подготовка | оба водителя на линии, тестовые заказы пассажира убраны, заказ создан через API | — |
| 1 | — | заказ режима «Предложение»: `isOfferOrderSnapshot`, межгородний класс, цена заказчика, не голосовой, `b_state = 1`, участников нет |
| 2 | пассажир открывает `/passenger-order` | заказ виден пассажиру |
| 3 | водитель 1 открывает список заказов | карточка **именно этого** заказа найдена по `b_id` |
| 4 | водитель 2 открывает список заказов | тот же заказ доступен и ему (**AC-2**) |
| 5 | водитель 1: «Предложить» → цена → «Отправить» | форма закрылась; на бэкенде `Considering` и **его** цена |
| 6 | водитель 2: то же со своей ценой | на бэкенде `Considering` и **его** цена |
| 7 | — | **AC-4**: предложений ровно два, цены не перепутаны, исполнителя нет, `b_state` не изменился |
| 8 | — | **AC-5**: пассажир видит предложение каждого водителя с его ценой |
| 9 | пассажир жмёт «Выбрать» у водителя 1 | водитель 1 → `Performer`, `b_state = 2` |
| 10 | — | исполнитель ровно один; у него сохранилось его предложение; проигравший не `Performer` |
| 11 | — | пассажиру показан водитель с тем же `u_id`, что на бэкенде |
| 12 | водитель 1 подтверждает уведомление «предложение принято» | окно со статусом `success` |
| 13 | водитель 1: карта → «Поехал» | backend, интерфейс водителя и пассажира — `Arrived` |
| 14 | водитель 1: «Приехал» | backend и оба интерфейса — `Started`, формы кода посадки не было |
| 15 | — | поездку выполняет ровно один водитель — тот, чьё предложение приняли, и по цене этого предложения |

## Чем А.1.3 отличается от А.1.2 в самом тесте

**Признак режима проверяется контрактом, а не полем.** `isOfferOrderSnapshot`
повторяет ту ветку `isOfferOrder()`, которая работает от данных заказа. Проверять
одно `b_location_class` было бы слабее: режим определяется парой признаков, и
заказ, потерявший любой из них, приложением «Предложением» уже не считается — а
значит, водительской формы предложения в интерфейсе просто не будет.

**Предложение — два шага в интерфейсе.** Первая кнопка открывает форму, вторая
отправляет; подписаны они одним ключом `DRIVER_OFFER_SEND`. Приложение
подтверждает приём не окном, а состоянием карточки — success-попап для
предложения оно сознательно не показывает (`CardModal.tsx`), поэтому признаком
принятого действия служит закрывшаяся форма, а доказательством — цена на
бэкенде.

**Цены у водителей разные и обе отличаются от цены заказчика.** Совпадающие
значения скрыли бы путаницу между водителями: тест проверяет не только «цена
дошла», но и «дошла цена именно этого водителя».

**После принятия предложения водитель получает модальное окно.** Этого шага нет
ни у А.1.1, ни у А.1.2. Приложение сообщает водителю, что его предложение
приняли (`pages/Driver/index.tsx`, `driver-offer-realtime-watch`), и пока окно
открыто, до карты не добраться — оно модальное. Тест подтверждает окно кликом,
как это делает водитель.

Оно же — независимое подтверждение режима: окно показывается **только** для
заказа, который приложение считает предложением (`isOfferOrder` внутри
watcher'а). Если бы заказ перестал опознаваться как А.1.3, окна бы не было.
Исход читается атрибутом `data-message-status`, а не заголовком.

**Кода посадки нет.** Переход `Arrived → Started` идёт прямым действием, как у
А.1.1. Тест проверяет это явно — до и после перехода утверждается, что формы кода
посадки не появилось. Без такой проверки тест молча прошёл бы и по чужому пути.

**Проигравший кандидат** остаётся записью участия. Замерено на gruzvill: он
остаётся `Considering` и его предложение сохраняется. Тест не требует конкретно
`Considering` — допускает `Considering` или `Canceled` и отдельно проверяет
главное: в поездку он не попал.

## Что показал первый прогон

Тест писался по снятому контракту, но одну особенность А.1.3 вскрыл только
прогон: окно «предложение принято» перекрывает карту, и клик «Поехал» до кнопки
не доходил. Это не дефект приложения и не помеха тесту — это шаг сценария,
которого нет у других режимов, и теперь он в сценарии есть.

## Инфраструктура теста

Фикстура, хелперы и селекторы, на которых написан сценарий выше. Всё опирается
на замеры из разделов ниже.

### Создание заказа

`createOfferOrder` (`e2e/fixtures/taxiApi.ts`) — тот же `orderPayload`, что у
остальных типов, плюс межгородний класс поездки:

```ts
{ ...orderPayload(options), b_location_class: INTERCITY_LOCATION_CLASS }
```

`b_options.customer_price` приходит из общего `orderPayload`, и вместе с классом
поездки это и есть признак режима. `set_confirm_state` не отправляется, метка
`[DRV:<id>]` не ставится — заказ должен быть виден обоим водителям.

`INTERCITY_LOCATION_CLASS = '2'` вынесена константой: если в конфигурации
изменится идентификатор межгородского класса, тест упадёт на проверке режима, а
не уедет молча на стандартный вызов.

### Хелперы независимой backend-проверки

Все — читающие: ни один не выполняет пользовательских действий и не содержит
логики сценария (§9 ТЗ).

| Хелпер | Что отвечает |
|---|---|
| `isOfferOrderSnapshot(order)` | опознает ли приложение заказ как А.1.3 — повторяет ту ветку `isOfferOrder()`, что работает от данных заказа |
| `offerPriceOf(order, driverId)` | цена предложения конкретного водителя (`c_options.performers_price`) |
| `offersOf(order)` | все предложения: чьё, за сколько, в каком состоянии участия |
| `customerPriceOf(order)` | цена, назначенная пассажиром |
| `orderOptionsOf(order)` | `b_options` объектом, откуда бы они ни пришли |

`isOfferOrderSnapshot` проверяет **контракт, а не поле**: заказ, потерявший любой
из двух признаков, приложением как «Предложение» уже не считается — и
водительской формы предложения в интерфейсе не будет. Поэтому AC-1 опирается
именно на него, а не на равенство `b_location_class`.

Цена — единственное значение предложения, которое возвращает backend. ETA и
комментарий проверяются только глазами интерфейса, независимой сверки для них
нет (см. TEST-E2E-004, «Что доступно для независимой проверки предложения»).

### Атрибуты и UI-хелперы предложения

| Файл | Атрибуты |
|---|---|
| `src/components/modals/CardModal.tsx` | `data-testid="driver-offer-open"`, `data-testid="driver-offer-price"`, `data-testid="driver-offer-send"` |
| `src/pages/Passenger/VotingForm.tsx` | `data-testid="passenger-candidate-offer-price"`, `data-offer-price` |

Зачем именно атрибуты, а не текст:

* **две кнопки с одинаковой подписью.** И «открыть форму предложения», и
  «отправить предложение» подписаны одним ключом `DRIVER_OFFER_SEND`. По тексту
  они неразличимы в принципе, а не только на другом языке;
* **цена предложения у пассажира.** Показанная строка отформатирована и содержит
  валюту из конфигурации бэкенда. Рядом отдаётся сырое значение
  `data-offer-price` — его и сверяют с `offerPriceOf`, доказывая, что до
  пассажира дошло предложение того самого водителя и с той самой ценой.
  Атрибуты появляются только в режиме предложения: у голосования эта же строка
  показывает время подачи, и подменять его ценой нельзя.

UI-хелперы: `openOfferForm`, `submitDriverOffer`, `isOfferFormVisible`
(`driverUi.ts`); `candidateOfferPrice`, `expectCandidateOfferPrice`
(`passengerUi.ts`).

Правки в `src/pages/Order/index.tsx` (второй, полноэкранной реализации той же
формы) сознательно **не делались**: сценарий идёт через карточку заказа, а
трогать production-код сверх необходимого для селекторов ТЗ запрещает.

Проверено на живом backend: `createOfferOrder` создаёт заказ, который
`isOfferOrderSnapshot` опознаёт как «Предложение»; два водителя дают разные
предложения, и `offerPriceOf`/`offersOf` читают их не перепутав; исполнителя
после предложений нет.

## Как приложение вообще отличает «Предложение»

Enum'а режимов нет: тип заказа — это предикат `isOfferOrder()`
(`src/tools/driverOffer.ts`). Он перебирает признаки по порядку, и первый же
сработавший решает дело. Два самых надёжных признака для E2E недоступны:

* **stored mode** (`localStorage.passengerChoiceOrderModes[b_id] === 'offer'`) —
  его ставит пассажирская форма после создания заказа. Заказ E2E создаётся через
  API, localStorage у ролей пуст, значит этого признака нет;
* **`b_cars_count = 0`** — легаси-признак старой схемы. На gruzvill не работает
  (замерено, см. таблицу).

Признак нужен не только ради AC-1. От него зависит, откроется ли вообще
водительская форма предложения (`shouldUseOfferFlow`, `CardModal.tsx`) и покажет
ли пассажир цену предложения (`requestIsOffer`, `VotingForm.tsx`). Без него
сценария А.1.3 в интерфейсе просто нет.

## Какие признаки переживают backend

Пассажирской учёткой создавался заказ на каждый вариант и тут же читался обратно
`/drive/get/{id}`. К снимку применялись backend-ветки `isOfferOrder()`.

| Вариант | Отправлено | Вернулось | Опознаётся как OFFER |
|---|---|---|---|
| A | `b_cars_count: 0` | `b_cars_count: "1"` | нет |
| B | `b_location_class: 2` + `b_options.customer_price` | **оба поля вернулись как есть** | **ДА** — ветка «customer_price + межгородний класс» |
| C | `b_only_offer: 1` | поля в ответе нет | нет (но заказ меняет поведение — см. ниже) |
| D | `b_options: { order_mode: 'OFFER' }` | `/drive` отказал: `wrong b_options keys: order_mode` | нет |
| E | `b_comments: ["95","96"]` | `b_comments: null` | нет |

**Рабочий признак ровно один — B.** Расширенный перебор комбинаций после него не
проводился: надёжный вариант найден, и он же прошёл весь сценарий целиком.

Почему остальные невозможны в принципе, а не «не получились»: список допустимых
ключей задан конфигурацией (`site_constants.b_options_valid_keys`), и `order_mode`
в нём нет — как нет и `offer_mode`/`driver_offer_mode`. То есть ветки 5 и 8
`isOfferOrder()` на gruzvill недостижимы никогда, а не в этот раз.

### Набор полей, которыми создаётся заказ

```
b_location_class: 2                 // «Межгородская поездка», alias countries_list
b_options: { customer_price: 150 }  // ключ разрешён b_options_valid_keys
b_car_class: <cc_id машины водителя>
b_max_waiting: 900
b_custom_comment: '[E2E <label>]'   // без [DRV:<id>] — см. ниже
```

`set_confirm_state` не отправляется: он нужен только голосованию. Проверено —
без него заказ доходит до водителей и принимает предложения.

Классы поездок берутся из конфигурации (`booking_location_classes`): `1` —
городская, **`2` — межгородская** (`alias: countries_list`), `3` — областная.
Межгородний **класс авто** — `51` (ГРАНД), но машины обоих тестовых водителей
класса `50` (ПЕТИТ, только город), поэтому признак строится на классе **поездки**,
а не автомобиля: `b_car_class: 51` увёл бы заказ от тестовых водителей.

Оговорка честности: это признак **косвенный** — «межгородская поездка с ценой
заказчика». Прямого флага режима gruzvill не отдаёт, и выбирать не из чего.

## `b_only_offer` — настоящее поле backend, которого приложение не видит

Отдельная находка, и её стоит знать до того, как кто-то решит использовать это
поле как «правильный» признак режима.

`b_only_offer: 1` backend **принимает и учитывает**: заказ создаётся сразу в
`b_state = 6` (`OfferedToDrivers`) вместо `1` (`Processing`). Замерено
чередованием пар, чтобы порядок создания не объяснял разницу вместо самого поля:

| Отправлено | `b_state` |
|---|---|
| `b_only_offer: 1` | `6` |
| без него | `1` |
| без него | `1` |
| `b_only_offer: 1` | `6` |

Но в ответе `/drive/get/{id}` этого поля **нет**, поэтому `isOfferOrder()` (ветка
`b_only_offer`) увидеть его не может никогда.

**Для сценария А.1.3 поле непригодно, и это тоже замер, а не опасение.** Заказ с
`b_only_offer: 1`:

* пропадает из `/drive/now` у обоих водителей (было `0` заказов против `1` у
  контрольного заказа в том же прогоне);
* отбивает предложение водителя ошибкой `not selected driver`.

То есть это другой поток — «заказ предложен заранее выбранным водителям», а не
«водители предлагают свои условия». Заказ А.1.3 им создавать нельзя.

Расхождение «backend поле принимает, но не возвращает, из-за чего фронтенд не
может определить режим» — это дефект backend, а не тестовой оснастки. По §11 ТЗ
он выносится отдельной задачей и внутри E2E-PR не чинится.

## Состояния сценария

Замерено на полном проходе: заказ → два предложения → выбор пассажира → поездка.

| Этап | `b_state` | водитель 1 | водитель 2 |
|---|---|---|---|
| заказ создан | `1` Processing | нет записи | нет записи |
| оба отправили предложения | `1` | `1` Considering | `1` Considering |
| пассажир выбрал водителя 1 | `2` Approved | `3` Performer | `1` Considering |
| водитель 1 отметил прибытие | `2` | `4` Arrived | `1` Considering |
| поездка началась | `2` | `5` Started | `1` Considering |

`b_state` сразу после создания — стабильно `1`: проверено отдельно, в том числе
когда оба водителя только что вышли на линию (четыре замера подряд, 0/3/6/11 с).
Значение `6` даёт только `b_only_offer`, который здесь не используется.

**Предложение не назначает исполнителя.** Оба водителя остаются `Considering`,
`b_state` не меняется — исполнителя создаёт только выбор пассажира. Этим А.1.3
отличается от А.1.1, где клик «Взять заказ» делает водителя `Performer` сразу.

## Что доступно для независимой проверки предложения

Отдельного массива предложений у заказа нет. Предложение — это запись участия в
`order.drivers[]`. Из значений предложения backend возвращает **только цену**:

```
drivers[i].c_options.performers_price   // 777 и 888 в замере — то, что ввёл водитель
```

Не вернулись **ни одного** из полей, которыми водитель дополняет предложение:
`c_pickup_time`, `c_arrival_time`, `c_comment`, `driver_offer_price` — все
`undefined` в ответе, хотя запрос принят с `status: success`.

Отдельно стоит отметить: конфигурация разрешает больше, чем приложение шлёт —
`c_options_valid_keys = {performers_price, driver_offer_comment, driver_offer_eta}`.
Приложение же кладёт в `c_options` только `performers_price`
(`buildDriverOfferOptions`, `src/API/order.ts`), а ETA и комментарий отправляет
верхнеуровневыми `c_pickup_time`/`c_comment`, которые не возвращаются. Менять это
ради теста нельзя — это production-логика.

**Следствие для Части 3:** независимо от UI проверяется **цена предложения и
идентификатор водителя**. ETA и комментарий backend-проверке не поддаются, и
строить на них утверждения нельзя.

## Проигравший кандидат

Замер, а не предположение: после выбора пассажира второй водитель остаётся
**`Considering(1)`**, и его предложение сохраняется целиком
(`c_options.performers_price` = введённая им цена). Backend его не снимает и не
отменяет.

Исполнитель при этом ровно один (`c_state === Performer` — единственная запись), и
в поездку проигравший не попадает ни на одном шаге: после `Started` в состоянии
`>= Performer` остаётся только выбранный водитель.

## Код посадки в А.1.3 не участвует

`Arrived → Started` проходит прямым действием: `set_start_state` **без**
`b_driver_code` возвращает `success` и переводит водителя в `Started(5)`.

Это совпадает с фронтендом: путь через код посадки гейтится `isVotingOrder(order)`,
а не `isOfferOrder` (`pages/Driver/Map.tsx`, `CardModal.tsx`). То есть шаги
прибытия и начала поездки у А.1.3 такие же, как у А.1.1, и переносить сюда
`openBoardingForm`/`submitBoardingCode` из А.1.2 нельзя.

`b_driver_code` в ответе заказа приходит пустой строкой.

## Роли и видимость

Нужны **три** учётки: пассажир и два водителя (`E2E_DRIVER2_*` — те же, что у
А.1.2). Два водителя здесь не для полноты картины: они закрывают вопрос ТЗ о
состоянии проигравшего кандидата и делают проверку «исполнитель ровно один»
осмысленной.

Заказ создаётся **без метки `[DRV:<id>]`** — по той же причине, что и в А.1.2:
метка прячет заказ от остальных водителей в режиме эмулятора, а А.1.3 нужен один
заказ, доступный обоим. Метка `[E2E <label>]` остаётся, и уборка работает по ней.

Замерено: заказ виден обоим водителям в `/drive/now`, оба отправляют предложения,
оба получают `success`.

## Чего backend не отдаёт — на это опираться нельзя

* `b_only_offer` — принят, но в ответе отсутствует;
* `b_cars_count` — сохраняется как `"1"` независимо от отправленного `0`;
* `b_comments` — вернулся `null`;
* `c_pickup_time`, `c_arrival_time`, `c_comment`, `driver_offer_price` у записи
  участия — `undefined` после успешной отправки предложения;
* `b_options.order_mode` и любые ключи вне `b_options_valid_keys` — заказ с ними
  не создаётся вообще.

Из второстепенного: `b_max_waiting: 900` возвращается без изменений — то самое
окно доверия пассажирского интерфейса (`0 < x ≤ 900`), что и у голосования.

---

# TEST-E2E-005 — А.1.4, отказ выбранного водителя

Негативная ветка предложения: водитель предложил свои условия → пассажир выбрал
именно его → **водитель отказался** → заказ не уезжает в поездку и возвращается в
поиск исполнителя.

Контракт отказа был **замерен на живом gruzvill до написания теста**, как требует
ТЗ: финальное `b_state` не задавалось заранее, а устанавливалось измерением. Ниже
— запуск, сценарий, прогоны, затем сам контракт и найденные ограничения
backend.

## Запуск

```bash
cd taxi
npm run test:e2e -- --project=offer-driver-decline
```

Нужны все три учётки, включая `E2E_DRIVER2_*` (раздел «Учётные данные»).

## Три независимые сессии

```
browser
├── passengerContext (storageState: e2e/.auth/passenger.json) → passengerPage
├── driver1Context   (storageState: e2e/.auth/driver.json)    → driver1Page  ← отказывается
└── driver2Context   (storageState: e2e/.auth/driver2.json)   → driver2Page  ← проверяет возврат в поиск
```

Общих cookies и localStorage у ролей нет.

## Сценарий и проверки

| Шаг | Действие | Проверка |
|---|---|---|
| подготовка | оба водителя на линии, тестовые заказы пассажира убраны, заказ создан через API | — |
| 1 | — | заказ режима «Предложение»: `isOfferOrderSnapshot`, межгородний класс, цена заказчика, не голосовой, `b_state = 1`, участников нет |
| 2 | пассажир открывает `/passenger-order` | заказ виден пассажиру |
| 3 | водитель 1 открывает список заказов | карточка **именно этого** заказа найдена по `b_id` |
| 4-6 | водитель 1: «Предложить» → цена → «Отправить» | форма открылась, введённая цена отображается, форма закрылась; на бэкенде `Considering` и **его** цена; исполнителя нет, `b_state` не изменился |
| 7 | — | пассажир видит предложение этого водителя с его ценой (сырое `data-offer-price`) |
| 8 | пассажир жмёт «Выбрать» | — |
| 9 | — | на бэкенде исполнитель ровно один и это он; `b_state = 2`; второй водитель в заказе не участвует; пассажиру показан тот же `u_id` |
| 10 | водитель 1 подтверждает окно «предложение принято» | формы предложения больше нет, **доступно действие отказа** |
| 11 | водитель 1 жмёт отказ → подтверждает в окне | действие выполнено через UI |
| 12 | — | **AC-06**: водитель → `Canceled`, у заказа исполнителя нет |
| 13 | — | **AC-07**: `b_state = 1` (не `Canceled`), ни одного водителя в `>= Performer`, заказ активен; предложение отказавшегося сохранилось |
| 14 | — | **AC-08**: пассажир не видит отказавшегося исполнителем, поездка не показана начавшейся |
| 15 | пассажир перезагружает страницу | **AC-09**: отказавшийся не вернулся исполнителем; на бэкенде по-прежнему `b_state = 1`, исполнителя нет, водитель `Canceled` |
| 16 | водитель 2: «Предложить» → цена → «Отправить» | заказ **снова принимает предложения**: водитель 2 → `Considering` со своей ценой, пассажир его видит |
| финал | — | отказавшийся так и в `Canceled`, исполнителя нет, поездка не началась, `b_state = 1` |

## Прогоны

Итог: **3 успешных прогона подряд** на финальном коде — 28.7 с, 26.5 с, 33.2 с,
без сбоев загрузки и без застрявших списков.

Путь до этого стоит описать, потому что два падения были содержательными и
изменили сам сценарий, а не только оснастку.

| Что упало | Где | Чем оказалось |
|---|---|---|
| карточка не появилась у второго водителя (шаг 16) | `openOrderCard(driver2)` | **дефект приложения** — опрос списка заказов встаёт навсегда; см. раздел ниже. Сценарий перестроен: список второму водителю открывается на шаге 4 и остаётся открытым |
| `passengerDriverId` виснет до таймаута | шаг 15 | **гонка в оснастке**: между «есть ли элемент» и «прочитать атрибут» панель успевает исчезнуть — как раз после отказа. Заменено на утверждения о локаторах, которые Playwright перепроверяет сам |
| заказ не виден пассажиру (шаг 2) | `expectOrderVisibleToPassenger` | нестабильность загрузки конфигурации приложения — то же, что у A.1.1–A.1.3, с тем же обходом `expectAppBooted` и `retries: 1` в CI |

Оба первых пункта разбирались замерами, а не перезапуском наугад. В частности,
для шага 16 отдельно измерено, что backend держит заказ доступным второму
водителю **устойчиво**, а не мгновение:

| Момент | Заказ в `/drive/now` у второго водителя |
|---|---|
| после предложения первого водителя | **да** |
| пока первый водитель — `Performer` | **нет** — заказ занят |
| после отказа, T+0 / +15с / +30с / +60с / +120с / +180с / +240с | **да** на всех замерах |

Отсюда и решение: карточку на шаге 16 нужно не догружать заново, а **дождаться её
возвращения в живом списке**. Заказ уходит из списка, когда занят, и возвращается,
когда освобождается, — это и есть повторный поиск, наблюдаемый глазами водителя.

## Полная сюита

Два прогона `npm run test:e2e` подряд: **9 passed / 2 failed** в каждом.

**A.1.1, A.1.2 и A.1.3 прошли в обоих прогонах** — правка `messageModalStatus` и
`confirmActionResult` (переход на поиск окна по видимости) регрессий не дала. Эти
два теста — единственные, кто кроме А.1.4 их использует.

Падения в обоих прогонах — инфраструктурные, и каждый раз в разных тестах,
включая существовавшие до этой задачи:

| Прогон | Кто упал | Причина |
|---|---|---|
| 1 | `driver-boarding B`, `offer-driver-decline` | `expectAppBooted` исчерпал 3 попытки — приложение осталось на «Database is unavailable», конфигурация с бэкенда не загрузилась. Сценарий не начинался: в диагностике ни участников, ни предложений |
| 2 | `driver-boarding A`, `offer-driver-decline` | `TypeError: fetch failed`, `SocketError: other side closed` — бэкенд закрыл соединение |

То есть это те же два известных фактора: нестабильность загрузки конфигурации
приложения (раздел «Известная нестабильность» выше) и обрывы со стороны бэкенда.
Локально `retries: 0`, поэтому одиночный сбой сразу красит прогон; в CI стоит
`retries: 1` — он рассчитан ровно на это.

Отдельно стоит знать про обрывы: `fetchWithConnectRetry` (`taxiApi.ts`)
сознательно повторяет запрос **только** когда соединение не установилось вовсе —
обрыв уже отправленного запроса не повторяется, потому что тогда нельзя
утверждать, что заказ не создался. Решение верное, и ослаблять его не стоит; цена
— такие прогоны краснеют.

## Инъекция регрессии

Проверено, что тест действительно падает от нарушения контракта, а не проходит
«по инерции». В `DriverCancelModal` временно убран вызов отмены — отказ водителя
перестал доходить до бэкенда. Результат:

```
Error: бэкенд снял участие отказавшегося водителя
Expected: 2      (Canceled)
Received: 3      (Performer)
```

То есть тест поймал ровно ту регрессию, которую называет ТЗ, — «водитель после
отказа остаётся Performer», — и упал на шаге 12, дойдя до него через все
предыдущие шаги. Инъекция откачена, production-диff содержит только два
`data-testid`.

## Чем А.1.4 отличается от А.1.3 в самом тесте

**Инвариант строится на паре `b_state` + `c_state`, а не на одном поле.**
Проверять только «водитель больше не Performer» было бы недостаточно: замерено,
что тот же `set_cancel_state`, отправленный **пассажиром**, даёт совсем другой
результат — заказ уходит в `Canceled(3)`, а водитель остаётся `Performer(3)`,
запись участия не сбрасывается. Различает эти два случая только пара полей.

**Исчезновение предложения не проверяется** — по контракту оно сохраняется.
Проверка «предложения больше нет» противоречила бы замеру, поэтому тест
утверждает обратное: цена отказавшегося осталась в записи участия.

**Возврат в поиск проверяется поведением, а не числом.** Совпадения `b_state = 1`
мало: оно не доказывает, что заказ действительно снова доступен. Поэтому шаг 16
даёт слово второму водителю — он отправляет предложение через интерфейс, и
пассажир его получает. Если бы заказ остался занятым отказавшимся водителем, этот
шаг был бы невозможен. Второй водитель здесь именно для этого, а не для полноты
картины.

**Сценарий не возвращается к списку заказов.** Карточка перерисовывается по
данным заказа и после выбора пассажира сама переходит в ветку `Performer` —
навигация не нужна. Это не оптимизация: при возврате к списку окно «предложение
принято» всплывает **заново** (watcher поднимает его при каждом монтировании,
пока водитель `Performer`), и оверлей перехватывает клики. Замерено probe.

**Факт клика по отказу ничего не доказывает.** `CancelDriverOrderModal` шлёт
запрос **без `await`** и уходит со страницы независимо от результата
(`DriverCancelModal.tsx:38-51`). Поэтому переход подтверждается опросом
состояния заказа, а не тем, что клик состоялся.

## Атрибуты, добавленные в приложение

| Файл | Атрибут |
|---|---|
| `src/components/modals/CardModal.tsx` (ветка `Performer`) | `data-testid="driver-order-cancel-open"` |
| `src/components/modals/DriverCancelModal.tsx` | `data-testid="driver-order-cancel-confirm"` |

Зачем именно атрибуты: у водителя-`Performer` в карточке три кнопки с **одним и
тем же классом** `order_take-order-btn`; у кнопки отказа нет ни подписи, ни
href, а иконка — инлайновый SVG без опознавательных признаков
(`components/Icon/index.tsx` — SVGR-компоненты). Отличить её можно было только
позицией, а позиционный селектор ТЗ запрещает. У кнопки подтверждения подпись
«OK» — по ней окно не отличить от других подтверждений.

Ветка `Arrived` той же карточки содержит такую же кнопку отмены, но атрибут туда
**не добавлялся**: в сценарий А.1.4 она не входит, а production-код правится
только под то, что тест действительно использует.

UI-хелперы: `orderCancelOpenButton`, `orderCancelConfirmButton`,
`isOrderCancelAvailable`, `declineAssignedOrder` (`driverUi.ts`).

## Правка существующей фикстуры: окно результата ищется по видимости

`messageModalStatus` и `confirmActionResult` (`driverUi.ts`) раньше проверяли
`count()` узла `[data-testid="message-modal"]`. Замерено, что этого мало: после
закрытия окна `success` в DOM остаётся **невидимый** узел с другим статусом, и
хелпер принимал его за показанное окно — клик уходил в невидимую кнопку и висел
до таймаута.

Теперь узел отбирается по видимости, а кнопка закрытия ищется внутри конкретного
окна, а не глобально. Для А.1.1–А.1.3 поведение не меняется: там окно
проверяется сразу после действия, когда оно видимо. Семантика при этом стала
честнее — «показанное водителю окно», а не «узел в разметке».

## Контракт отказа — замерено на живом gruzvill

Водитель `u871`. Backend-часть воспроизведена на трёх заказах (`15646`, `15648`,
`15650`), UI-часть снята на заказе `15651`. Отказ выполнялся **кликом в
интерфейсе**; приложение отправляет `POST /drive/get/{b_id}` с
`action=set_cancel_state` и **без** `reason`.

| Этап | `b_state` | `performer` | водитель `c_state` | offer |
|---|---|---|---|---|
| заказ создан | `Processing(1)` | none | нет записи | нет |
| предложение отправлено | `Processing(1)` | none | `Considering(1)` | `731` |
| пассажир выбрал водителя | `Approved(2)` | `u871` | `Performer(3)` | `731` |
| **водитель отказался** | **`Processing(1)`** | **none** | **`Canceled(2)`** | `731` |

Ответ backend на отказ — `HTTP 200 {"code":"200","status":"success"}`. Заказ
остаётся в списке активных заказов пассажира.

**Заказ возвращается в фазу поиска** — гипотеза ТЗ про повторный поиск
подтвердилась замером. Переход мгновенный: он виден уже в T+0, и на T+2s
состояние то же; отдельного ожидания асинхронной сходимости тесту не
потребуется. Назначение `Performer` после выбора пассажира занимает ~236 мс
через API и ~798 мс через UI — отсюда берутся timeout'ы `expect.poll`.

Два следствия для будущих проверок:

* **предложение отказавшегося не удаляется** — запись участия остаётся с
  `c_state = Canceled(2)` и прежней ценой. Проверять «предложения больше нет»
  нельзя;
* **инвариант строится на паре `b_state` + `c_state`.** Замерено, что тот же
  `set_cancel_state` от **пассажира** даёт другой результат: заказ уходит в
  `Canceled(3)`, а водитель остаётся `Performer(3)` — запись участия не
  сбрасывается. Одного «водитель больше не Performer» для A.1.4 недостаточно.

## UI-путь отказа — тоже замерен

Цепочка записана наблюдением за сетью драйверской страницы, без подмены
запросов:

```
клик по третьей кнопке карточки → окно «Cancel Order Confirmation?» → «OK»
        ↓
POST /drive/get/{b_id}   multipart:  action=set_cancel_state   (reason отсутствует)
        ↓
HTTP 200 {"code":"200","status":"success"}
        ↓
b_state=Processing(1) | performer=none | driver=Canceled(2)
```

То есть UI-путь ведёт ровно к тому же запросу — отказ можно и нужно выполнять
кликом, а не вызовом endpoint из теста.

У водителя-`Performer` в карточке ровно три кнопки, и **ни у одной нет
`data-testid`**:

| # | текст | что это |
|---|---|---|
| 0 | `""` | WhatsApp-иконка |
| 1 | `Arrived` | «Приехал» |
| 2 | `""` | **отказ** → окно подтверждения |

Класс у всех трёх одинаковый (`order_take-order-btn`), иконки — инлайновые SVG
без опознавательных атрибутов. Поэтому Этапу 2 нужны два атрибута: на кнопку
отказа (`CardModal.tsx:1816-1821`) и на «OK» в окне подтверждения
(`DriverCancelModal.tsx:69-73`).

Два замера, которые определяют, как вести сценарий:

* **карточка остаётся открытой** после выбора пассажира — она перерисовывается в
  ветку `Performer` по данным заказа. Возвращаться к списку заказов не нужно, и
  это важно: при возврате окно «предложение принято» всплывает заново (watcher
  поднимает его при каждом монтировании, пока водитель `Performer`);
* **пассажир после reload** не видит отказавшегося исполнителем — панель
  водителя не отрисовывается, заказ остаётся активным. Старое frontend-состояние
  не восстанавливается (AC-09).

## Почему выбран именно этот сценарий (и почему это не GAP)

А.1.1 проверяет прямое назначение, А.1.2 — конкурентный выбор, А.1.3 —
конкурентные ценовые предложения. Все три проверяют УСПЕШНОЕ назначение.
Следующий по ценности E2E — не четвёртый вариант успеха, а ветка отказа, потому
что она проверяет инвариант FSM, которого не проверяет ни один существующий тест:

> выбор кандидата сам по себе не гарантирует начала поездки, если выбранный
> водитель затем отказывается.

## Что установлено по коду

**Модель `driver_declined` из `src/tools/driverOffer.ts` для E2E непригодна.**
Пять состояний (`waiting`, `client_selected_this_driver`,
`client_selected_other_driver`, `driver_confirmed`, `driver_declined`) живут
только в `localStorage['driverOfferClientResponseSimulation']`; «клиент выбрал
вас» решается хешем `orderId + userId` по локальному таймеру, без сети;
`declineClientSelectedOffer` не делает ни одного запроса; и вся ветка выключена
флагом `shouldSimulateOfferClientResponse = false` (`CardModal.tsx:942`,
`pages/Order/index.tsx:581`). То есть наличие модели — не доказательство
live-backend контракта, о чём ТЗ и предупреждало.

**Что доступно водителю-`Performer` на самом деле.** После выбора пассажиром
offer-ветка карточки исчезает (`isOfferAcceptedForDriver = c_state >= Performer`,
`shouldUseOfferFlow = isOfferMode && !isOfferAssignedTrip`), и остаётся ветка
`c_state === Performer` (`CardModal.tsx:1802-1822`): WhatsApp, «Приехал» и иконка,
открывающая `CancelDriverOrderModal`. Последняя — единственный путь отказа, и она
шлёт `set_cancel_state` **без** `reason` (`reason` отбрасывает `addToFormData`).
Путь «Отменить и скрыть» доступен только ДО выбора, а «Отказаться от заказа» —
только в режиме голосования.

**Отдельно про надёжность.** `CancelDriverOrderModal` вызывает отмену БЕЗ `await`
и уходит со страницы независимо от результата (`DriverCancelModal.tsx:38-51`).
Факт клика поэтому ничего не доказывает — переход обязан подтверждаться
независимым чтением backend.

## Как снимался контракт

Контракт установлен **до написания теста**, как требует ТЗ, временной
разведочной оснасткой. В репозитории её нет: она удалена перед PR, потому что
контракт она не проверяет, а устанавливает, и повторно её запускать незачем — всё
измеренное перенесено в разделы выше.

Что она делала:

* **backend**: проходила цепочку «заказ → предложение → выбор пассажира → отказ»
  запросами и снимала HTTP-ответ отказа **отдельно** от состояния, затем снимки в
  T+0 / T+2s / T+5s / T+10s. Отдельный ответ нужен был, чтобы различить четыре
  исхода: запрос принят и состояние изменилось; принят, но меняется асинхронно;
  отклонён; технически `success`, а бизнес-состояние не изменилось. Перед отказом
  стояло ожидание фактического `Performer` — иначе измерялся бы переход
  `Considering → Canceled`, то есть совсем другой контракт;
* **интерфейс**: доводила заказ до `Performer` кликами и записывала реальный
  сетевой обмен клика по отказу — наблюдением, без подмены запросов. Так
  установлена цепочка «клик → endpoint → payload → результат», и только после
  этого стало можно утверждать, что UI-путь отказа существует;
* **диагностика падения теста**: когда первый прогон упал на шаге 16, та же
  оснастка проверила, видит ли второй водитель заказ, — и показала, что видит
  (раздел «Прогоны»).

Ответ, который надо было получить: снимает `set_cancel_state` только участие
водителя, отменяет весь заказ, или backend его отклоняет. Третий исход означал бы
GAP и закрытие задачи без теста. Фактический ответ — первый: **снимается только
участие, заказ возвращается в поиск**. Backend-часть воспроизведена на трёх
заказах, UI-часть — на четвёртом.

Разведка потребовала отдельной защиты: оснастка создавала на живом backend
настоящие заказы, поэтому её изменяющие запросы были закрыты явным флагом
окружения, а один случайный прогон (флаг `--list` не прошёл через
`run-script-os`) и стал причиной эту защиту сделать. Это же объясняет, почему в
репозитории оснастки нет: держать в дереве то, что молча создаёт заказы,
незачем.

# Общее для всех тестов

## Артефакты при падении

`trace`, `screenshot`, `video` — в `e2e-results/artifacts/`, HTML-отчёт в
`playwright-report/`. Оба каталога в `.gitignore`.

## Изоляция

Каждый прогон создаёт свой заказ, помеченный `[DRV:<id>]` — механизм самого
приложения, по которому водитель в режиме внешнего эмулятора видит только свои
тестовые заказы. После теста заказ отменяется (`afterEach`), аккаунты не
зависают. Подметание зависших заказов ограничено заказами с тестовыми метками —
см. «Изоляция и уборка» выше.

Тесты идут строго по одному (`workers: 1`): они делят одного тестового водителя
и его машину.

## CI

`.github/workflows/e2e.yml` — job **Playwright E2E**, запускается на каждый
Pull Request и на push в `main`. Он выполняет ровно ту же команду, что и
разработчик локально:

```
GitHub Actions → npm ci → playwright install chromium → npm run test:e2e
                                                              │
                                              npm start (localhost:3000)
                                                              │
                                          живой backend gruzvill
```

Отдельной CI-реализации сценария нет: один тестовый код, два способа запуска.
`workers: 1`, `retries: 1` и сбор артефактов приходят из `playwright.config.ts`
по переменной `CI`, которую GitHub выставляет сам, — в workflow они не
дублируются.

### Секреты

Учётные данные в CI берутся только из GitHub Secrets репозитория (Settings →
Secrets and variables → Actions). Ключи те же, что в `taxi/.env.e2e.local`:

```
E2E_DRIVER_LOGIN      E2E_PASSENGER_LOGIN      E2E_API_BASE
E2E_DRIVER_PASSWORD   E2E_PASSENGER_PASSWORD
E2E_DRIVER_TYPE       E2E_PASSENGER_TYPE
```

Аккаунты должны быть **тестовыми**: прогон создаёт настоящие заказы. В коде и в
workflow нет ни одного значения — только `process.env.*`.

PR из форка секретов не получает: там тест упадёт с понятным сообщением из
`fixtures/accounts.ts`. Это не маскируется — `continue-on-error` в workflow нет.

### Очередь

Прогоны выстроены в общую очередь (`concurrency`, без отмены текущего): тесты
делят одного тестового водителя и его машину, и два параллельных прогона ломали
бы состояние заказа друг другу.

### Артефакты и диагностика

Артефакты выгружаются всегда (`playwright-e2e-<run>-<attempt>`, 7 дней):
`taxi/playwright-report/` и `taxi/e2e-results/`. Выгрузка на любом исходе нужна
для §15 ТЗ: если первая попытка упала, а вторая прошла, это должно быть видно в
отчёте, а не спрятано за retry.

При падении в лог job уходит строка `E2E FAILURE DIAGNOSTICS` с `orderId`,
`driverId`, `carId` и состоянием заказа на бэкенде. Если отменить заказ не
удалось — `E2E CLEANUP FAILED: orderId=…`: по этому номеру заказ можно найти и
удалить руками. Токены, пароли и cookie в диагностику не попадают.

`taxi/e2e/.auth/` (там storageState с токеном сессии) в артефакты не выгружается.
Проект `setup` идёт вообще без trace/video/screenshot: он вводит логин и пароль в
форму приложения, а trace хранит само значение `fill()`.

Одного этого мало: к любому упавшему тесту Playwright безусловно прикладывает
`error-context.md` — ARIA-снимок страницы, в котором **и логин, и пароль видны
открытым текстом** (проверено инъекцией падения после ввода). Настройкой это не
отключается, поэтому у `setup` вложение снимает репортёр
`e2e/reporters/stripSetupErrorContext.ts`: он стоит первым в списке, поэтому копия
не доходит ни до HTML-, ни до JSON-отчёта, а файл удаляется с диска. Сообщение об
ошибке и стек при этом остаются целиком. Проверка: сломать селектор кнопки входа в
`auth.setup.ts`, прогнать `npx playwright test --project=setup` и поискать значения
учёток в `e2e-results/` и `playwright-report/` — совпадений быть не должно.

Trace самого сценария содержит сетевой обмен приложения, включая токен сессии
тестового водителя, — поэтому срок хранения артефактов ограничен, а учётки для CI
берутся только тестовые.

### Required check

Сделать **Playwright E2E** обязательной проверкой для `main` — отдельный шаг в
Settings → Branches, он требует прав администратора репозитория.
